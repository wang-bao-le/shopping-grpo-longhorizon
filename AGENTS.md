# Repository contract

This repository supports one workflow only:

```text
Baseline → SFT → GRPO → Evaluation
```

The runtime contract is ShopSimulator Environment v2.1, Reward v3, observation
v2 and tool schema v2. Do not add compatibility launchers, historical datasets,
old benchmarks, machine-specific paths or experiment journals.

Training data must never overlap `data/evaluation/tasks.jsonl`. Strict success
requires a complete `gold_purchase` terminal result with `reward_valid=true`.

Do not start training, merge models or run the 200-task evaluation unless the
user explicitly requests execution.

# shopping-grpo-longhorizon 工作规范

## 1. 适用范围与修改边界

本规范适用于本仓库的 Python、Shell、配置与文档。仓库当前包含数学答案解析与奖励、Prompt、vLLM 推理和基线评测、结果保存及启动脚本；也为后续数据处理、训练和 RL 代码提供通用约束。

- 修改前先阅读目标文件、相关调用点、测试和项目文档；遵循已有接口、评测协议与文件布局。
- 只改与任务直接相关的文件和行。不要顺带重构、格式化或清理无关代码。
- 保持项目自有代码的注释和 docstring 简洁、准确的中文；API、参数名、库名和通用术语可保留英文。
- 修改实现时同步更新受影响的注释、文档和测试；不要保留与现状不符的说明。
- 第三方代码保留其许可证、来源和必要注释，除非任务明确要求，否则不批量翻译或改写。

## 2. 教学友好的代码与注释规范

代码和注释共同服务于理解。优先让函数职责、变量命名和文件结构反映真实数据流；复杂算法、训练或评测流程应让初学者能从 docstring、阶段注释和关键原因说明顺着读下去。

### 代码组织

- 一个函数优先完成一个清晰职责，并让输入、主要变换和输出按执行顺序出现；不要为了教学把无关职责塞进同一个函数。
- 复杂流水线将可复用的数据变换、数值计算与流程编排分开。训练脚本负责组织数据、优化和保存，底层工具函数负责可独立测试的张量或文本变换。
- 变量名应表达数据所处阶段，例如 `prompt_ids`、`labels`、`response_mask`、`token_log_probs`；不要用难以追踪数据来源的单字母或泛化名称。
- 不为了形式引入单次使用的类、配置层或抽象。教学性来自清晰边界和可追踪数据流，而不是更多层级。

### 简单代码

简单赋值、直观判断、常规循环和 API 调用通常不需要注释。不要逐行翻译 `return`、`backward()`、`optimizer.step()` 等已自解释的语句。

### 复杂函数与长流程

- 复杂函数、公开函数或行为不直观的函数必须使用中文 docstring，说明职责、核心输入/输出、关键语义或副作用；必要时用简短的数据流或 shape 帮助定位，但无需机械复述类型注解和函数名。
- 训练、评测、数据处理、保存和其他具有多个主要阶段的流程，使用连续的阶段注释展示执行顺序。每个阶段说明目标、产出或关键约束，而不是复述下一行代码。
- 当函数包含多个实质阶段时，优先使用 `# 1.`、`# 2.` 等编号帮助读者建立流程；短函数不要为了形式拆分编号。
- 文件较长且存在明确职责区域时，才使用模块分区注释；短小或单一职责模块不需要分隔线。

### 核心函数的教学型 docstring

训练、评测、数据处理、张量变换和数学计算等包含非直观逻辑的核心函数，应使用适度详细的教学型 docstring。目标是让初学者理解“函数做什么、核心原理是什么、关键输入输出如何使用”，而不是仅凭 docstring 复现全部实现过程。

- **职责与核心思想**：用简洁段落说明函数在整体算法或流水线中的作用，以及输入如何在概念上变成输出；不要完整复述函数体的每个阶段。
- **必要公式与行为差异**：只保留理解函数行为所必需的公式、归一化口径或可选分支，例如 bool 参数不同取值对应的核心计算差异。
- **参数说明**：需要结构化说明时使用 `Args:`，解释重要参数的算法语义、关键 shape、排列约束和必要取值要求；不要穷举实现内部才需要的细节或重复显而易见的类型信息。
- **返回值说明**：需要结构化说明时使用 `Returns:`，解释各返回值的含义、关键 shape 和主要用途；dict 或 metrics 只说明调用者需要理解的核心内容，不必逐项罗列所有可推导字段。
- **关键约束**：只记录调用者若不了解就可能误用接口或得到错误结果的约束；reshape、广播、局部变量、异常分支和特殊情况等具体实现优先交给函数体阶段注释。

docstring 负责说明“是什么、核心怎么算、如何正确调用”；函数体内的编号阶段注释负责说明“代码执行到这里具体在做什么、为什么这样实现”。两者不得大段重复同一流程或细节。

不要为了显得教学友好而加入完整算法教程、逐步转述实现、穷举所有异常或与当前接口无关的背景知识。docstring 的长度应与接口复杂度匹配；简单函数仍使用简短 docstring，不强制套用完整的 `Args:` / `Returns:` 模板。

### 模块级职责分区

当一个较长 Python 文件包含多个稳定、明确的顶层职责区域时，使用三级分隔注释帮助读者定位代码所在部分：

```python
# ========================================
# 数据加载
# ========================================
```

- 适合的职责区域包括：默认配置与常量、数据结构、辅助函数、数据加载、训练、验证、日志与结果保存、参数解析和程序入口。
- 分区名称应简短、稳定并描述实际职责；同一文件使用同一种分隔格式，不用过细的步骤名称替代职责名称。
- 仅在文件确实有多个顶层职责区域时使用。短小文件、单一职责模块或只有少量函数的工具文件不强制添加分区。
- 分区回答“当前位于文件的哪一部分”；函数内部的教学型阶段注释回答“当前流程在处理什么”。函数内部不得使用分区横线替代阶段注释。

### 教学型流程注释

训练、评测、数据处理、张量变换和数学计算等核心函数，即使单条语句本身直观，也应使用教学型流程注释让读者追踪数据如何变化。此规则优先于“简单代码通常不注释”，但只适用于核心流程。

- docstring 先给出函数职责、核心思想和必要的简短数据流；完整执行阶段及其实现细节由函数体内的阶段注释承担。
- 函数内部按真实执行顺序划分阶段，并在阶段开始处说明当前处理什么、输入经过该阶段后得到什么。
- 对影响正确性的关键语句解释当前实现的原因和约束，例如 add_special_tokens=False、EOS 的监督归属、Prompt 不参与 loss、mask 与 labels 的对齐或 Causal LM shift。
- 不要求逐行翻译简单赋值、append 或直观 API 调用；但不能因代码本身可读而省略整个处理阶段的数据流说明。
- 优先说明当前项目的真实约束。只有确实存在兼容性差异时，才补充泛化提醒或替代行为。

### 数学、张量与 ML 逻辑

对不显然的算法或数值实现，在相关代码附近说明作用、原因和遗漏/错位的后果。特别关注：

- Causal LM shift：说明输入、标签和相关 mask 如何同步错位。
- mask 与 padding：说明哪些 token 参与计算，以及错误对齐会如何改变 loss 或指标。
- tensor shape：仅在维度变化、广播、索引或聚合容易出错时标注关键 shape。
- loss normalization：说明分母的统计口径，以及不同 batch 或 token 数量下的影响。
- 数值稳定性：说明使用 `log_softmax`、`logsumexp` 等形式避免下溢、溢出或 NaN 的原因。
- gradient accumulation：说明 loss 缩放与实际优化批次的关系，避免重复或遗漏缩放。

这些说明应与当前实现对应；它们是复杂 ML 代码的参考重点，不是所有函数都必须套用的模板。

### 注释质量

- 注释优先解释 why、约束和不直观的实现细节，同时保留足以理解算法主流程的阶段说明。
- 不把注释写成长篇教程，也不为了简洁而省略关键的数据流、数学语义或流程边界。
- 注释、docstring、参数、返回值、异常和副作用必须与实现一致。

### 英文术语与代码标识符

- 中文注释和 docstring 中可以直接使用英文技术术语、变量名、参数名和函数名，不需要为了强调而机械添加反引号。
- prompt、rollout、reward、advantage、policy、group mean、group std、shape 等普通技术术语直接书写；它们属于自然语言的一部分，不视为必须突出显示的代码片段。
- 在 `Args:`、`Returns:` 或上下文已经明确时，参数名、返回值名称和函数名通常直接书写，不重复使用反引号强调。
- 只有确实需要区分代码字面量与自然语言时才使用反引号，例如 `True`、`False`、`None`、具体配置值、完整表达式或容易产生歧义的代码片段。
- 数学或代码公式需要突出时，优先将完整表达式作为一个整体展示；不要分别给公式中的每个英文变量添加行内代码标记。
- 完成修改前复查本次新增或改动的注释与 docstring，移除仅用于装饰英文单词的反引号，避免密集的行内代码标记降低阅读连贯性。

### 中文注释与 docstring 换行

- 中文注释和 docstring 必须按完整语义单元换行，语义完整性高于 Python、Black、Ruff、PEP 8 或编辑器的固定行宽习惯。
- 不得仅因一行超过 88、100、120 或其他字符数而拆分完整中文句子；即使超过常规行宽，一个完整句子原则上也保持在同一行。
- 不得在逗号、顿号、介词、修饰语、参数名与其解释、shape 与所属张量、API 与其说明、数学表达式与其解释、以及“返回……为……”等尚未表达完整的位置插入源码换行。
- 只有在完整句号、分号、独立列表项或相对独立的步骤、条件、职责之间才主动换行；结构化的参数和返回值说明可以按项目分行。
- 代码格式化工具的默认行宽不能作为拆分中文注释或 docstring 的理由。若格式化结果破坏中文语义单元，应保留或恢复符合本节规则的源码行。
- 完成修改前必须复查本次新增或改动的中文注释与 docstring，合并所有仅因固定宽度产生的断行；不得使用 awk length、formatter 行宽或类似机械检查替代语义审查。

## 3. 评测、数据与实验协议

- Prompt 模板、chat template、采样参数、stop sequences、随机种子、答案解析方式、数据划分和指标口径属于评测协议。比较实验时，未经明确授权不得随意改变。
- 修改评测或推理代码时，检查批次输出顺序、解析失败处理、可复现性和结果统计口径。
- 逐样本结果与汇总结果应各自说明用途；保存内容应支持复核核心指标和协议，而不泄露密钥或凭据。
- 不将模型权重、数据集缓存、实验产物、访问令牌或其他大文件/敏感文件加入提交，除非用户明确授权并已核对范围。

## 4. 测试与验证

验证强度与改动风险匹配，优先选择能直接证明本次改动的最小检查。不能执行时，明确报告未验证内容和原因，不用无关检查替代。

| 改动类型 | 默认验证 |
| --- | --- |
| Markdown、配置说明或 AGENTS.md | 查看相关 diff；暂存后执行一次 `git diff --cached --check` |
| 仅 Python 注释或 docstring | 查看相关 diff；执行 `python -m py_compile`；默认不运行完整测试 |
| 小范围 Python 逻辑 | 查看相关 diff，并运行最直接相关的测试或小型冒烟测试 |
| 训练、评测、数据或推理逻辑 | 检查协议和相关 diff，运行最小样本冒烟测试 |
| 依赖、环境或启动配置 | 只运行能直接验证该配置的同步、导入或启动检查 |

完整评测、训练和模型下载耗时较长，只有用户明确要求或最小验证不足以覆盖风险时才执行。

## 5. Git、提交与推送

- 只有用户明确要求时才 `git commit` 或 `git push`；不使用 `--amend`、`--force` 或 `--force-with-lease`，除非用户明确授权并确认影响。
- 提交前先用 `git status --short --branch` 确认范围，只查看并暂存本次任务相关的明确路径；不要使用 `git add .` 或 `git add -A`。
- 按第 4 节完成必要验证后，检查 `git diff --cached` 并执行一次 `git diff --cached --check`，再提交和普通推送。
- 推送后检查一次 `git status --short --branch`，分别报告目标提交是否同步与工作区是否仍有无关改动。
- 不提交未经授权的结果、模型、缓存、密钥或临时文件。